# Fleet host config for bench/fleet.sh — whitespace-separated, '#' comments.
#
# Fields:
#   name        short label used on the command line
#   ssh_target  ssh destination (user@host, or a Host alias from ~/.ssh/config);
#               use "local" to run on this machine without ssh
#   goos        GOOS to cross-compile for this host (linux | darwin)
#   goarch      GOARCH (amd64 | arm64)
#   remote_dir  scratch dir on the remote for the pushed binary
#   model_dir   directory on the remote that holds the .gguf models
#
# Edit ssh_target and model_dir to match your setup, then:
#   ./bench/fleet.sh check all
#
# name    ssh_target   goos    goarch  remote_dir        model_dir
m2max     m2max        darwin  arm64   /tmp/sllm-bench   ~/models
m5max     m5max        darwin  arm64   /tmp/sllm-bench   ~/models
9900x     9900x        linux   amd64   /tmp/sllm-bench   ~/models
i5        i58500t      linux   amd64   /tmp/sllm-bench   ~/models
x5675     x5675        linux   amd64   /tmp/sllm-bench   ~/models
4700u     local        linux   amd64   /tmp/sllm-bench   /home/martin/projects/scriptling-llm-lib/models
# Windows hosts: OpenSSH with the default cmd.exe shell; use forward-slash paths.
# win11     winbox       windows amd64   C:/Users/me/sllm  C:/Users/me/models
# winarm    winarm       windows arm64   C:/Users/me/sllm  C:/Users/me/models
